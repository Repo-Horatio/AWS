Internet Gateway

aws ec2 attach-internet-gateway -vpc-id "VPC-ID" --internet-gateway-id "IGW-ID" --region [] // attach an Internet Gateway to a VPC(a VPC can be attached to one Internet Gateway only)